<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <title><%= typeof title !== 'undefined' ? title : 'Hedef Takip Uygulaması' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-light">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark px-3">
    <a class="navbar-brand" style="text-decoration: none;">Hedef Sayar</a>
    <% if (user) { %>
      <div class="ms-auto d-flex align-items-center gap-2">
  <span class="text-white me-3">Hoş geldin, <%= user.name %></span>

  <form action="/dashboard" method="get">
    <button type="submit" class="btn btn-outline-light btn-sm">Dashboard</button>
  </form>

  <form action="/goals" method="get">
    <button type="submit" class="btn btn-outline-light btn-sm">Hedeflerim</button>
  </form>

  <form action="/goals/new" method="get">
    <button type="submit" class="btn btn-outline-light btn-sm">Hedef Ekle</button>
  </form>

  <form action="/logout" method="get">
    <button type="submit" class="btn btn-outline-danger btn-sm">Çıkış Yap</button>
  </form>
</div>

    <% } %>
  </nav>

  <div class="container mt-4">
    <% if (error) { %>
      <div class="alert alert-danger"><%= error %></div>
    <% } %>
    
    <%- body %>
  </div>
</body>
</html>